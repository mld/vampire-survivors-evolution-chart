{
  "name": "vampire-survivors-evolution-chart",
  "version": "0.0.1",
  "description": "Extract icons from items.json/items.png.",
  "main": "extraxt-items.js",
  "scripts": {
    "extract": "node extract-items.js",
    "convert": "node convert-items.js",
    "build": "node build-index.js",
    "all": "npm run extract && npm run convert && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mustache": "^4.2.0",
    "sharp": "^0.32.6"
  }
}
